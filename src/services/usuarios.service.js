// Logica de negocio de E1 que NO es autenticacion.
//
// El ciclo de sesion (registro, login, logout, recuperacion de contrasena) lo
// resuelve Better Auth en /api/auth/*, no se implementa aca. Este modulo queda
// para los endpoints propios de la epica: gestion de miembros y roles (HU-4) y
// auditoria de accesos (HU-5).
